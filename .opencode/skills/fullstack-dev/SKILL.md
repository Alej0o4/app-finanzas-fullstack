---
name: fullstack-dev
description: Arranca backend y frontend en orden para desarrollo local
---
1. Backend primero (ejecutar skill backend-startup).
2. Frontend después: `pnpm dev` desde `frontend/`.
3. Verificar: `curl http://localhost:8000/` y abrir `http://localhost:3000`.
